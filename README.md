# Test-websites

Sandbox de diseño de UmanoAI para probar tendencias visuales (ej. héroes 3D/WebGL, scroll-storytelling) **antes** de tocar un repo o sitio de producción.

## Reglas

- Nada acá se deploya a un dominio real hasta que el dueño lo apruebe explícitamente.
- Cada prototipo vive en su propia carpeta (`/agricheck`, etc.), en el mismo stack que el sitio de producción al que apunta — así portar el ganador es copiar la carpeta, no migrar build tooling.
- Todo prototipo debe pasar el Loop de Verificación Visual del agente `diseno-web` (chrome-devtools screenshots desktop+mobile + Lighthouse ≥80/90/90) antes de proponerse para producción.
- Todo elemento 3D/WebGL necesita fallback estático para mobile/low-end y respetar `prefers-reduced-motion`.

## Prototipos

- `/agricheck` — rediseño del sitio de agrichecksrl.com (repo real: `Juan-457/AgriCheck`), con el hero 3D de la mazorca de maíz como pieza central de la home. 35 páginas HTML/CSS/JS estático sin build tooling. **Alcance del rediseño (decisión del dueño): solo 4 páginas** — `index.html` (home), `nosotros.html`, `productos.html` (catálogo/listado filtrable) y `contacto.html` — con identidad visual propia (verde monte + dorado trigo + papel crudo) sobre la tipografía Lato original, sin cambios. Estado: **verificado** en esas 4 páginas — pasó el Loop de Verificación Visual (screenshots desktop 1440×900 + mobile 390×844, Lighthouse Performance 87-100 / Accessibility 95-100 / Best Practices 100 en las páginas muestreadas; SEO ronda 60-63 solo por el `noindex` intencional del sandbox). Pendiente: revisión del dueño antes de considerarlo candidato a producción; ver notas de `pendiente_verificar` en el registro de tarea del agente `diseno-web`.
  - **Las 31 fichas individuales de producto NO están rediseñadas — son copia fiel 1:1 del original** (madex, zumsil, checkmate-\*, tundrabac, t-gro\*, biolure, nembac42, phosbac, vitisan, parka, plutex, subvert, beauvisan, atroverde, magnet, biomagnet, nexula, superfifty-prime, amyprotec-42, pack-soja, v12-initiate, etc.). El dueño de UmanoAI cortó el alcance: esas páginas las desarrolla y mantiene el propio cliente AgriCheck, y no quiere que el rediseño se aparte de lo que ellos gestionan. Cada ficha se copió byte a byte desde `/root/AgriCheck` (mismo layout, contenido, paleta original `--brand-green`/`--brand-brown`, Tailwind CDN, JS específico de cada página — ej. tabs/comparativas inline — y sus propios `assets/llm-signals.js` y `assets/whatsapp-float.js`), junto con las imágenes/PDF que cada una referencia, para que funcionen standalone dentro de esta carpeta. El catálogo (`productos.html`, sí rediseñado) enlaza a estas fichas por su nombre de archivo real.
  - Nota heredada del original (no introducida acá, ya rota en `agrichecksrl.com` en producción): 5 imágenes/PDF con rutas relativas a carpetas que no existen en el repo (`../Cultiva/Parka/cerezas (11).jpg` en `parka.html`, `../fichas/Ficha_Phosbac.pdf` en `phosbac.html`, `../img/BioAtlantis/SuperFifty PRIME/banner superfifty fhd.png` en `superfifty-prime.html`, `../Andermatt/Madumbi/V12 Initiate/v12-bottle-mockup.png` en `v12-initiate.html`, `Programas cultivos/Soja/Hero pack soja.jpeg` en `pack-soja.html`) — confirmado 404 también en el sitio real. Fuera de alcance: son fichas que mantiene el cliente.
